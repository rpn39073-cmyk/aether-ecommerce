{
    "name": "aether-backend",
    "version": "1.0.0",
    "description": "Root deployment package",
    "scripts": {
        "start": "node server/index.js",
        "build:server": "cd server && npm install",
        "build:client": "cd client && npm install && npm run build",
        "build": "npm run build:server && npm run build:client"
    }
}